@using MARS.Models.Entity
@model List<DegisiklikIzleme>
@{
    ViewBag.Title = "DegisiklikIndex";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>Değişiklik Sayfaları</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="#">AnaSayfa</a>
            </li>
            <li class="breadcrumb-item active">
                <strong>Değişiklik Sayfaları</strong>
            </li>
        </ol>
    </div>
    <div class="col-lg-2">

    </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox ">
                <div class="ibox-title">
                    <h4>Değişiklik Sayfaları</h4>
                </div>
                <div class="ibox-content">

                    <div class="table-responsive">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Dokuman No</th>
                                    <th>Sürüm Tarihi</th>
                                    <th>Sürüm No</th>
                                    <th>Dosyalar</th>
                                </tr>
                            </thead>
                            @foreach (var d in Model)
                            {
                                <tbody>
                                    <tr>
                                        <td>@d.DokumanListe.TekdokNo</td>
                                        <td>@d.SurumTarihi</td>
                                        <td>@d.SurumNo</td>  
                                        <td>
                                            @Html.ActionLink("Download", "Download", new { fileName = d.DegisiklikDosyaYolu })
                                        </td>


                                    </tr>
                                </tbody>
                            }
                        </table>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

